%head
  %link(rel="stylesheet" href="/index.css")

%body
  %h1 The Philosopher Developer

  .content
    - if @post
      .post
        = markdown(@post.content).gsub("--", "&mdash;")
      %a(href="/") Back
    - else
      - @posts.each do |p|
        %p
          %a(href="/#{p.id}")= p.title
