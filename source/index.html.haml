---
pageable: true
per_page: 10
---

- page_articles.each do |article|
  %article
    %header
      %h1
        = article.title
        .date= article.friendly_date

    = article.summary(1000, '...')

    %p= link_to('Read more', article.url)

%p
  = link_to('Previous', prev_page.url) if prev_page
  = link_to('Next', next_page.url) if next_page
